# ADK Agent Project

![Generated by The AI Language](https://img.shields.io/badge/Generated%20by-The%20AI%20Language-indigo)

This project was visually designed and generated using [The AI Language Agent Builder](https://theailanguage.com/builder).

## 🛠️ Git Setup (Crucial Step)

1.  **Clone the repository** (if you haven't already):
    ```bash
    git clone https://github.com/theailanguage/agent_builder_nocode
    ```

2.  **Navigate to this workflow**:
    ```bash
    cd workflow_1
    ```

3.  **Get Updates**:
    If you made changes in the visual builder and pushed again, update your local code:
    ```bash
    git pull
    ```

---

## 🔑 Getting your Google API Key

To run these agents, you need a Gemini API Key.

1.  Go to [Google AI Studio](https://aistudio.google.com/).
2.  Click on **Get API Key** in the top left corner.
3.  Click **Create API Key**.
4.  You can get a **Free Tier** key by selecting an existing Google Cloud Project.
5.  Copy the key and paste it into the `agents/.env` file in this project.

---

## 📺 Video Tutorials

Follow our step-by-step video guide to set up Python, UV, and VS Code on your operating system.

### 📦 Installation
*   **[🍎 macOS Setup](https://www.youtube.com/watch?v=htZBIwtEvOE&t=236s)** (03:56)
*   **[🪟 Windows Setup](https://www.youtube.com/watch?v=htZBIwtEvOE&t=536s)** (08:56)
*   **[🐧 Ubuntu Setup](https://www.youtube.com/watch?v=htZBIwtEvOE&t=972s)** (16:12)

### 🚀 Running the Agent
    ```bash
    cd agents

    # 1. Setup .env
    cp .env.example .env
    # (Edit .env with your API Key)

    # 2. Init Environment
    uv init
    uv venv
    source .venv/bin/activate
    uv add google-adk

    # 3. Run
    adk run .   
   ```

---

## 🚀 Quick Start

### Prerequisites
- Python 3.11+
- `uv` package manager (Recommended)

### Running the Agent
1. Install dependencies:
   ```bash
   uv sync
   ```
2. Ensure your API Key is set in `agents/.env`.
3. Run the root agent:
   ```bash
   adk run agents/root_agent.yaml
   ```

## 📂 Structure

- `agents/root_agent.yaml`: The main orchestration flow (Entry Point).
- `agents/*.yaml`: Individual agent definitions referenced by the root.
- `agents/.env`: Environment variables for the agent runtime.

---
*Built with ❤️ by [The AI Language](https://theailanguage.com)*